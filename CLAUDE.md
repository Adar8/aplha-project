# AlphaTrader Learn

אפליקציה ללימוד שוק ההון למתחילים. כל הממשק בעברית ומימין לשמאל (RTL).

## סטאק

- **React 19** + **Vite 8** (JavaScript, JSX — בלי TypeScript בשלב זה)
- **CSS רגיל** — קובץ CSS לכל קומפוננטה, טוקנים גלובליים ב-`src/styles/tokens.css`. אין Tailwind / CSS-in-JS.
- **פונטים** מ-Google Fonts, נטענים ב-`index.html`.
- אין עדיין ראוטר, ניהול state או בדיקות — להוסיף רק כשיש בהם צורך אמיתי.

### פקודות

```bash
npm install      # התקנת תלויות
npm run dev      # שרת פיתוח (http://localhost:5173)
npm run build    # בילד לפרודקשן ל-dist/ — חייב לעבור לפני כל קומיט
npm run preview  # הרצת הבילד מקומית
```

### מבנה

```
index.html                 # lang="he" dir="rtl", טעינת פונטים
src/
  main.jsx                 # נקודת כניסה, טוען tokens.css ו-global.css
  App.jsx                  # שלד הדף
  components/              # קומפוננטה + קובץ CSS באותו שם (Header.jsx / Header.css)
  data/modules.js          # רשימת מודולי הלימוד — מקור האמת לדף הבית
  styles/tokens.css        # צבעים, פונטים, מרווחים, רדיוסים
  styles/global.css        # reset, טיפוגרפיה בסיסית, .container
```

## שפה עיצובית

**אווירה:** טרמינל מסחר ניאוני אבל ידידותי למתחילים — רקע כהה ורגוע, הדגשות צבעוניות וזוהרות במינון.

### צבעים (`src/styles/tokens.css`)

| טוקן | ערך | שימוש |
|---|---|---|
| `--color-bg` | `#0b1433` | רקע ראשי — **כחול-נייבי כהה, לעולם לא שחור** |
| `--color-surface` | `#131f47` | כרטיסים ומשטחים |
| `--color-border` | `#26366b` | גבולות |
| `--color-text` / `--color-text-muted` | `#eef2ff` / `#9aa6d1` | טקסט ראשי / משני |
| `--color-cyan` | `#00f0ff` | טורקיז חשמלי — הדגשה ראשית, פעולות, עלייה |
| `--color-pink` | `#ff2e97` | ורוד בוהק — דגש, ירידה, אזהרה |
| `--color-purple` | `#b04dff` | סגול זוהר — משני, גרדיאנטים |
| `--color-lime` | `#b6ff3b` | ליים — הצלחה, תגיות, נתונים חיוביים |

- לכל צבע הדגשה יש `--glow-*` (box-shadow) לאפקט זוהר — ל-hover ולאלמנטים בולטים בלבד, במינון.
- כרטיס מודול מקבל `accent` (`cyan` | `pink` | `purple` | `lime`) שקובע את צבע הפס העליון, המספר והזוהר.

### טיפוגרפיה

| פונט | טוקן | שימוש |
|---|---|---|
| **Rubik** | `--font-heading` | כותרות (h1–h4), לוגו |
| **Heebo** | `--font-body` | טקסט רץ |
| **JetBrains Mono** | `--font-mono` | נתונים: מספרים, מחירים, אחוזים, טיקרים, מספרי מודולים |

## כללי עבודה

1. **RTL תמיד.** להשתמש ב-logical properties (`margin-inline`, `padding-inline-start`, `inset-inline-end`) ולא ב-`left`/`right`. טקסט לטיני או מספרים שצריכים כיוון LTR (טיקרים, מחירים, הלוגו) — לעטוף ב-`dir="ltr"`.
2. **כל הטקסט למשתמש בעברית.** מונחים מקצועיים באנגלית מותר להוסיף בסוגריים אחרי המונח העברי.
3. **בלי ערכי צבע/פונט קשיחים בקומפוננטות** — רק `var(--token)`. צבע חדש? מוסיפים אותו קודם ל-`tokens.css`.
4. **קומפוננטה = קובץ JSX + קובץ CSS** באותו שם, עם שמות מחלקות בסגנון BEM (`module-card__title`, `module-card--pink`).
5. **תוכן כנתונים.** מודולים ותוכן לימודי נשמרים ב-`src/data/`, לא מקודדים בתוך JSX.
6. **נגישות:** HTML סמנטי (`section`, `article`, `ol`), `aria-labelledby` לסקשנים, `:focus-visible` נראה, כבוד ל-`prefers-reduced-motion`.
7. **רספונסיביות:** לבדוק ברוחב מובייל (~390px) ודסקטופ. בלי גלילה אופקית.
8. **תוכן לימודי בלבד** — לא לנסח שום דבר כהמלצת השקעה. הדיסקליימר בפוטר נשאר.
9. לפני קומיט: `npm run build` עובר בלי שגיאות.

## סטטוס

- ✅ דף בית: כותרת, הסבר קצר, רשימת מודולים ככרטיסים (סטטוס "בקרוב", בלי תוכן).
- ⏳ מודול 1 — מה זו מניה
- ⏳ מודול 2 — סוגי פקודות
