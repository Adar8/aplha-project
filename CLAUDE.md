# AlphaTrader Learn — הקשר מלא

## מה הפרויקט
אפליקציה מקצועית ללימוד שוק ההון, בעברית, למתחילים. מתחילה ככלי לימוד אישי ובהמשך אמורה להפוך למוצר בתשלום. המשתמש יודע קצת לסחור אבל חסר לו סדר, ולכן התוכן בנוי מהיסוד ובסדר הגיוני. בונים בהדרגה, מודול אחרי מודול.

## מיצוב המוצר (חשוב)
- אנחנו **לא** ייעוץ השקעות. לא ממליצים על ניירות ערך, לא אומרים למשתמש מה לקנות או מתי, ולא מבצעים מסחר. אף אחד לא שם אצלנו כסף.
- אנחנו שני דברים:
  1. **מערכת ללימוד עצמי** של שוק ההון: איך הוא עובד, מושגים, כלים. מלמדים גם כלים שסוחרים משתמשים בהם (גרפים, סטופ, גודל פוזיציה), כהסבר ולא כהוראה.
  2. **ממשק נוח יותר לכסף שלך**: משתמש שמשקיע אצל ברוקר אחר (בלינק, אינטראקטיב וכו׳) יכול, אם ירצה, להזין אילו מניות קנה ובאיזה סכום, ולראות מה הכסף שלו עשה מאז.
- כל ניסוח חדש נבדק מול זה: מסבירים איך דברים עובדים, לא אומרים מה לעשות.

## החלטות שהתקבלו
- **התאמה אישית**: שאלון פתיחה (ניסיון, מטרה, זמן שבועי, ולא חובה: גיל וסכום השקעה) ומבחן מיקום. כל מודול נפתח ברמה שמתאימה למשתמש (בסיסי / מעמיק), אבל **כל הרמות תמיד זמינות** בכל מודול ובכל שלב.
- **אזור שוק** (חדשות, גרפים חיים, מניות במעקב, תיק): ארה״ב בלבד בגרסה הראשונה, על בסיס ווידג'טים של TradingView.
- **חשבונות משתמש**: כבר עכשיו (תיק, מעקב ופרופיל לימוד מסונכרנים בין מכשירים). עד שיהיה שרת, הכול נשמר מקומית דרך `src/progress/storage.js`.

## שפה וכיוון
- כל התוכן בעברית, dir="rtl"
- מונחים מקצועיים: בעברית, עם המונח באנגלית בסוגריים
- הסברים בעברית טבעית ומדוברת, לא תרגום מילולי מאנגלית

## שפה עיצובית (לא לשנות בלי בקשה מפורשת)
עודכנה ב-26.9.2026 אחרי שאלון עיצוב עם בעל האתר: ״כמו משחק, אבל אסתטי״, עם מענה גם למבוגרים.
- **תחושה**: פלטפורמת מסחר מודרנית עם אלמנטים של משחק (אריחים צבעוניים, התקדמות בשלבים, הישגים). נקי ולא ילדותי
- **רקע**: בהיר בגוון כחלחל (`#eef2f9`), פאנלים לבנים. **כחול נייבי** (`#0f1e3d`) לניווט הצד, לכותרת של כל שיעור ולפאנל הראשי בלוח הבקרה
- **מצב כהה** (כפתור בתפריט): נייבי עמוק (`#0b1428`)
- **צבע ראשי**: כחול (`#2458d6`; על נייבי: תכלת `#86adff`). כפתור ראשי אחד מלא, השאר עם מסגרת
- **שוק**: ירוק לעלייה, אדום לירידה. נרות בסגנון פלטפורמות מסחר (`--chart-up` / `--chart-down`)
- **צבעי משחק**: שישה גוונים (`--tone-1`…`--tone-6`: כחול, טורקיז כהה, ענבר, אדום, ירוק, כחול-ים) לאריח של כל מודול ולהישגים. **אין גוונים סגולים**
- **פונט**: Assistant לכל האתר (מוגש מהשרת של האתר, לא מ-Google), גוף 17px. מספרים ב-`tabular-nums`, בלי פונט מונו
- **פינות**: 4px לכפתורים ושדות (`--radius-control`), 6px לפאנלים. **בלי כפתורים מעוגלים**
- **אפקטים**: קצרים ועדינים: כניסה של כרטיסים, הרמה במעבר עכבר, מילוי שלבי התקדמות, חגיגה קצרה בסימון ״הושלם״. בלי זוהר, בלי גרדיאנטים ובלי אנימציות גלילה. הכול כבוי ב-`prefers-reduced-motion` ובסגנון ״רגוע״
- **סגנון תצוגה**: ״תוסס״ (ברירת מחדל) או ״רגוע״ (טקסט גדול יותר, בלי תנועה). ״רגוע״ הוא ברירת המחדל למי שציין בשאלון גיל 50 ומעלה; תמיד אפשר להחליף בתפריט. `data-style` על `<html>`
- **טקסט**: תוויות בעברית ורק איפה שצריך, בלי תגיות באנגלית באותיות גדולות, בלי חיצים ונקודות אמצע לקישוט, בלי מספור 01/02
- **פאנלים רק לכלים** (סימולטורים, מחשבונים, לוח הבקרה). טקסט ההסבר זורם בלי קופסאות
- **הדגשות (Callout)**: פס צבע בצד ורקע עדין. כחול למידע, ענבר לאזהרה
- **פשוט / מעמיק**: שורה מתקפלת עם קו דק
- **ניווט**: תפריט צד קבוע בדסקטופ (אייקוני קו רק בתפריט), פס עליון עם כפתור ״תפריט״ במסך צר
- הצבעים מוגדרים כמשתני CSS ב-`src/styles/tokens.css` בלבד

## כללים שאסור להפר (מבעל האתר)
האתר לא אמור להיראות כאילו נוצר בבינה מלאכותית. לכן, לעולם:
- בלי גוונים סגולים ובלי כפתורים מעוגלים.
- בלי המלצות פיקטיביות (Testimonials) ובלי נתונים שקריים. כל מספר באתר מחושב מהנתונים או אמיתי. נתוני סימולציה מסומנים במפורש כדמיוניים.
- בלי כותרת ראשית כללית ("לומדים X בקלות"). כותרות קונקרטיות שמתארות את התוכן בפועל.
- בלי אייקוני אימוג׳י, ובלי סימנים שמוצגים כאימוג׳י (▶ ⏸ ⚠ ✓ וכו'). טקסט רגיל.
- בלי אנימציות מוגזמות בגלילה, בלי אנימציות לסמן העכבר, בלי ספירות לאחור שקריות.
- בלי כתיבה שנשמעת כמו בינה מלאכותית או גנרית: בלי שאלות רטוריות בפתיחה, בלי ״בואו נ...״, בלי סיסמאות. משפטים ענייניים עם פרטים.
- בלי תמונות שנראות כאילו נוצרו ב-AI.
- בלי שום אזכור לכך שהאתר נוצר בעזרת AI, בתוכן האתר ובקבצים שמגיעים לגולש.
- הצהרת נגישות, מדיניות פרטיות ותקנון תמיד קיימים ומקושרים מהפוטר של כל עמוד. **אסור לשכוח את הצהרת הנגישות.** כל שינוי באתר שמשפיע על נגישות מחייב עדכון שלה ובדיקה מחדש.

## לפני עלייה לאוויר (לא מעלים לפני שכולם מתקיימים)
- [ ] דומיין אמיתי משלנו מחובר
- [ ] כל הפרטים ב-`src/data/site.js` מולאו בפרטים אמיתיים (דומיין, מפעיל, דוא״ל, עיר שיפוט, רכז/ת נגישות)
- [ ] `npm run check:launch` עובר
- [ ] התקנון ומדיניות הפרטיות נבדקו על ידי עורך/ת דין
- [ ] בדיקת נגישות עם קורא מסך, ורצוי גם אצל מורשה נגישות שירות; הצהרת הנגישות עודכנה בהתאם
- [x] פביקון עם לוגו האתר (`public/favicon.*`, `apple-touch-icon.png`, `icon-*.png`)
- [x] דף מדיניות פרטיות, תקנון והצהרת נגישות

## סטאק
- React + Vite
- כל מודול הוא קומפוננטה/עמוד נפרד
- שמירת התקדמות (מודול הושלם + תאריך) ב-localStorage, עטוף ב-try/catch

## מבנה התוכן
כל מודול כולל: הסבר, לפחות אלמנט אינטראקטיבי אחד, וכפתור "סמן כהושלם".

- [x] 01 — מהי מניה: מה זה אומר להיות בעלים, מה זו בורסה, שוק ראשוני מול משני, Bid/Ask. סימולטור: סליידרים של לחץ קונים מול מוכרים שיוצרים נר עולה או יורד בזמן אמת.
- [x] 02 — סוגי פקודות: Market מול Limit, כולל סימולטור ספר פקודות
- [x] 03 — קריאת גרף: טווחי זמן, מגמות, תמיכה והתנגדות, נפח מסחר. תרגיל זיהוי מגמה + סימולטור סימון רמות עם חשיפת ההמשך.
- [x] 04 — ניהול סיכונים: אסימטריית הפסדים, סטופ, גודל פוזיציה, יחס סיכון-סיכוי ותוחלת. מחשבון החזרה, מחשבון גודל פוזיציה + סימולטור עקומות הון (אותו "מזל", סיכון שונה).
- [x] 05 — ניתוח יסודי ומכפילים: שלושת הדוחות, מכפילים (P/E, P/B, P/S, ROE, חוב להון), מלכודת ערך. בונה דוח רווח והפסד + בלש מכפילים (4 חברות, נטרול רווח חד-פעמי, שאלות).
- [x] 06 — חקירת מניה: תהליך מחקר יסודי בארבע שאלות (העסק, איכות, חוסן, מה המחיר מניח), מקורות (10-K, 10-Q, 8-K, שיחות ועידה), חפיר כלכלי, איכות רווח ותגמול במניות, חוב נטו וכיסוי ריבית. מחשבון DCF הפוך + מעבדת חקירה של חברה דמיונית שנגמרת ב״דף מחקר״ (בלי המלצה).
- [x] מילון מונחים: 290 מונחים, חיפוש וסינון לפי 13 קטגוריות — כלי השקעה וקרנות, ניירות ערך, השוק האמריקאי, מסחר ותנודתיות, ניתוח טכני, ניהול סיכונים, מדדי הערכה, דוחות כספיים, מאקרו, מיסוי, משכנתאות, פסיכולוגיה של משקיעים, גופי פיקוח
- [x] התאמה אישית: שאלון פתיחה + מבחן מיקום, מסלול מומלץ וזמן לימוד אמיתי בדף הבית, בורר רמה (בסיסי / מעמיק) בכל מודול, ושכבת ״במילים פשוטות״ + ״להעמקה״ בכל סעיף.
- [x] התיק שלי (גרסה מקומית): הזנת עסקאות קנייה/מכירה שבוצעו אצל ברוקר אחר (מספר מניות או סכום בדולרים, כולל מניות שבריות), מחיר נוכחי ידני עם תאריך עדכון, עלות ממוצעת, רווח/הפסד ממומש ולא ממומש, משקל בתיק. נשמר בדפדפן בלבד.
- [ ] התיק שלי: מחירים אוטומטיים (ספק נתונים + שרת) וגרף TradingView לכל מניה (לא נבדק עדיין: סביבת הפיתוח חוסמת את TradingView)
- [ ] חשבונות משתמש (שרת + מסד נתונים)
- [ ] אזור שוק (ארה״ב): חדשות, פתיחת מסחר, גרפים חיים, מניות במעקב, התיק שלי

## מפת דרכים
- חודשים 1–2: MVP — מילון + 3–4 מודולים אינטראקטיביים
- חודשים 3–5: מנוע התקדמות, בחנים, מורה AI מובנה
- חודשים 6–8: כל התוכן + עיצוב סופי
- חודשים 9–12: עלייה לאוויר אמיתית, שרת, מסד נתונים ותשלומים

## כללי עבודה
- לא לדווח על שינוי שלא בוצע בפועל. בסוף כל משימה לפרט אילו קבצים השתנו.
- כל משימה בבראנץ' נפרד, עם PR בסוף.
- לשמור על עקביות עיצובית בין כל המודולים.

---

## המשימה עכשיו
1. שמור את כל הטקסט הזה כקובץ CLAUDE.md בשורש הריפו.
2. הקם את שלד האפליקציה (React + Vite) עם דף בית: כותרת, הסבר קצר, וכרטיסים לכל המודולים ולמילון.
3. בנה את מודול 01 במלואו, כולל הסימולטור.

---

## נספח טכני (איך הקוד בנוי בפועל)

### פקודות
```bash
npm install      # התקנת תלויות
npm run dev      # שרת פיתוח (http://localhost:5173)
npm run build    # בילד לפרודקשן — חייב לעבור לפני כל קומיט
npm run preview  # הרצת הבילד מקומית
npm run check:launch  # חייב לעבור לפני עלייה לאוויר
```

### מבנה
```
index.html                        # lang="he" dir="rtl", טעינת פונטים
src/
  App.jsx                         # ראוטים (react-router)
  data/modules.js                 # רשימת המודולים + המילון — מקור האמת לדף הבית
  data/glossary.js                # הקטגוריות ומונחי הבסיס של המילון (TERMS = בסיס + glossaryMore.js)
  data/glossaryMore.js            # מונחים נוספים (שוק אמריקאי, ניתוח טכני, סיכונים, דוחות, מיסוי, פסיכולוגיה ועוד)
  glossary/                       # עמוד המילון: חיפוש (search.js), כרטיס מונח, עיצוב
  pages/                          # HomePage (לוח הבקרה), achievements.js (הישגים מחושבים מהנתונים), NotFound
  components/                     # רכיבים משותפים: Sidebar (ניווט צד), Logo, ModuleLayout, Callout, Term, CompleteButton
  theme/                          # מצב כהה וסגנון תצוגה (תוסס/רגוע) — theme.js
  modules/<slug>/                 # כל מודול בתיקייה משלו: עמוד + סימולטורים + CSS
  hooks/                          # הוקים משותפים (useCompact)
  lib/                            # כלים משותפים ללא React (seededRandom — אקראיות דטרמיניסטית)
  progress/                       # שמירת התקדמות ופרופיל ב-localStorage (storage.js) + Context/Hook
  profile/                        # פרופיל לימוד: שאלות, רמות, מסלול (profile.js), מבחן מיקום (placement.js), Context, Simple/Deep
  onboarding/                     # עמוד השאלון (/start) ופאנל ״המסלול שלך״ בדף הבית
  legal/                          # מדיניות פרטיות (/privacy), תקנון (/terms), הצהרת נגישות (/accessibility)
  portfolio/                      # ״התיק שלי״ (/portfolio): חישובים טהורים (portfolio.js), Context, טופס עסקה, עמוד
  data/site.js                    # פרטי האתר והמפעיל לעמודים המשפטיים. null = חסר (מוצג כ״חסר״ ונכשל ב-check:launch)
scripts/check-launch.mjs          # בדיקה לפני עלייה לאוויר: npm run check:launch
  styles/tokens.css               # כל הצבעים, הפונטים, המרווחים — המקום היחיד להגדרתם
  styles/global.css               # reset, טיפוגרפיה בסיסית
```

### הוספת מודול חדש
1. רשומה ב-`src/data/modules.js` (`id`, `number`, `title`, `summary`, `accent`, `path`, `status: 'available'`, `minutes: { basic, deep }`, ו-`why` עם משפט לכל מטרה: `understand` / `longterm` / `active` / `track`). להוסיף את ה-id גם ל-`ORDER_BY_GOAL` ב-`src/profile/profile.js`, ושתי שאלות ל-`PLACEMENT` ב-`src/profile/placement.js`.
2. תיקייה `src/modules/<slug>/` עם עמוד שעטוף ב-`<ModuleLayout module={...}>` — הוא מספק כותרת, בורר רמה, ״למה זה רלוונטי אליך״, ניווט וכפתור "סמן כהושלם".
   בכל סעיף: בלוק `<Simple>` (הסבר פשוט, עם אנלוגיה מהיום-יום) ובלוק `<Deep>` (ניואנסים, נוסחאות, מושגים מתקדמים) מ-`src/profile/Depth.jsx`. הבלוק שמתאים לרמה פתוח, והשני מקופל אבל זמין.
3. Route ב-`src/App.jsx`.

### הוספת מונח למילון
רשומה ב-`MORE_TERMS` שב-`src/data/glossaryMore.js` (או בבסיס שב-`glossary.js`): `id` (באנגלית, ייחודי — משמש כעוגן `/glossary#id`), `he`, `en`, `category`, `definition`, ולא חובה: `example`, `aliases` (שמות נוספים לחיפוש), `related` (ids של מונחים קשורים), `module` (id של מודול שמסביר את המונח לעומק). כשמודול חדש מלמד מונח — להוסיף לו `module`.

### מוסכמות
- טוקני צבע: `--color-bg` / `--color-bg-deep` / `--color-surface` / `--color-border`, `--color-text` / `--color-heading` / `--color-text-muted`, `--color-accent` (וגם `--color-cta` לכפתור הראשי), `--color-navy` / `--color-on-navy` / `--color-on-navy-muted` / `--color-on-navy-accent` לאזורים הנייביים, `--tone-1`…`--tone-6` ו-`--color-on-tone` לאריחים, `--color-up` / `--color-down` לעלייה וירידה, `--chart-*` לגרפים, `--color-info` / `--color-warning` להדגשות. השמות הישנים (`--color-cyan` / `pink` / `amber` / `lime`) ממופים לפלטה החדשה עד שכל המודולים יעברו. פינות: `--radius-control` לכפתורים ושדות, `--radius-sm` / `--radius-lg` לפאנלים. מצב כהה: `:root[data-theme='dark']`, סגנון רגוע: `:root[data-style='calm']`.
- בלי ערכי צבע או פונט קשיחים בקומפוננטות — רק `var(--token)`.
- RTL: להשתמש ב-logical properties (`margin-inline`, `inset-inline-start`). גרפים, מחירים וטיקרים עטופים ב-`dir="ltr"`. ב-`<svg>` צריך גם `direction: ltr` ב-CSS — המאפיין `dir` לא משפיע עליו, וטקסט הצירים "גדל" לתוך הגרף.
- מונח מקצועי מוצג עם `<Term he="..." en="..." />` כדי שיהיה עקבי בכל המודולים.
- מחלקות משותפות: צבעי טקסט `.is-up` / `.is-down` / `.is-lime` / `.is-amber` / `.is-muted` ב-`global.css`; `.widget` (סימולטור), `.bullets`, `.takeaways`, `.btn`, `.callout`, `.chip`, `.segmented` ב-`ModuleLayout.css`. הוק `useCompact` (מסך צר) ב-`src/hooks/`. מודול חדש משתמש בהן ולא מגדיר מחדש.
- כל גישה ל-localStorage עוברת דרך `src/progress/storage.js` (עטוף ב-try/catch). מידע חדש שנשמר על המשתמש מחייב עדכון של מדיניות הפרטיות ושל כפתור ״מחיקת הנתונים שלי״.
- רמת השיעור הנוכחי: `useLessonLevel()`, פרופיל ותשובות: `useProfile()`. תשובות השאלון משמשות רק להתאמת הלימוד (רמה, סדר, זמן, דוגמאות, סכומי ברירת מחדל בסימולטורים), אף פעם לא להמלצה.
- קומפוננטה = קובץ JSX + קובץ CSS באותו שם, מחלקות בסגנון BEM.
- נגישות (תקן ישראלי 5568 / WCAG 2.1 AA): כל עמוד קורא ל-`useDocumentTitle`; טבלה שנגללת לרוחב מקבלת `tabIndex={0} role="region" aria-label`; טקסט צבעוני עומד בניגודיות 4.5:1 גם על רקעים צבועים; תנועה אוטומטית נעצרת כש-`prefers-reduced-motion`. אחרי שינוי: axe-core על כל העמודים (מחשב + טלפון), בדיקת מקלדת, והגדלה ל-200% ול-320px בלי גלילה לרוחב.
- תוכן לימודי בלבד — לא לנסח שום דבר כהמלצת השקעה.
